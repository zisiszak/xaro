import { type RequestHandler } from 'express';
// import { type ContentInfo, getContentInfo } from '~/_modules/index.js';

export const AboutMediaController: RequestHandler = async () => {
	// const contentInfo = await getContentInfo(req.contentID!, req.userAccessToken!.id);
	// if (!contentInfo) return void res.status(404).end();
	// res.status(200).json(contentInfo).end();
};
