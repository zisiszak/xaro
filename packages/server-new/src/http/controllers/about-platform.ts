import { type RequestHandler } from 'express';
// import { readPlatformEntry } from '~/models/database/access.js';

export const AboutPlatformController: RequestHandler = async () => {
	// const platform = await readPlatformEntry(req.platformID!);
	// if (!platform) return void res.status(404).end();
	// res.status(200).json(platform).end();
};
