import { type RequestHandler } from 'express';
// import { readPlatformProfileEntry } from '~/models/database/access.js';

export const AboutPlatformProfileController: RequestHandler = async () => {
	// const platformProfile = await readPlatformProfileEntry(req.platformProfileID!);
	// if (!platformProfile) return void res.status(404).end();
	// res.status(200).json(platformProfile).end();
};
