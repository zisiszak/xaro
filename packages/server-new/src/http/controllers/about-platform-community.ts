import { type RequestHandler } from 'express';
// import { readPlatformCommunityEntry } from '~/models/database/access.js';

export const AboutPlatformCommunityController: RequestHandler = async () => {
	// const platformCommunity = await readPlatformCommunityEntry(req.platformCommunityID!);
	// if (!platformCommunity) return res.status(404).end();
	// res.status(200).json(platformCommunity).end();
};
