import styled from 'styled-components';
import { factor } from '../../styleguide/factor';

export const Navbar = styled.nav`
	margin: 0;
	border: unset;
	overflow: hidden;
	font-size: ${factor.SM(1)};
	padding: ${factor.SM(1)} ${factor.LG(1)};
`;
